import express,{Request,Response,NextFunction} from "express";

class App{
    // private port:number;
    // private middlewares;
    // private routes;

    // constructor(port:number, middlewares, routes){
    //     this.port = port;
    // }
}